<section class="hero">
  <div class="hero-content">
    <h1 class="hero-title" style="color: #422222;">Descubre tu aroma ideal</h1>
    <p class="hero-text">
      Prueba perfumes de diseñador sin pagar de más. Decants 100% originales, listos para ayudarte a encontrar tu fragancia perfecta.
    </p>

    <div class="hero-buttons">
      <a href="https://santuredecants.mitiendanube.com/" class="btn-primary">Ver catálogo</a>
      <a href="#educacion" class="btn-secondary">¿Qué son los decants?</a>
    </div>
  </div>
</section>

<style>
  /* Layout safety for this component only */
  .hero,
  .hero * {
    box-sizing: border-box;
  }

  .hero {
    width: 100%;
    min-height: 90vh;
    background: #f8f8ef;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0 20px;
    text-align: center;
    overflow-x: hidden; /* evita overflow horizontal */
    overflow-y: visible;
  }

  .hero-content {
    max-width: 800px;
    width: 100%; /* importante para no exceder ancho */
  }

  .hero-title {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 20px;
    line-height: 1.05;
    word-break: break-word; /* rompe palabras largas si hiciera falta */
    overflow-wrap: break-word;
  }

  .hero-text {
    font-size: 1.25rem;
    color: #444;
    margin-bottom: 35px;
    line-height: 1.6;
    word-break: break-word;
    overflow-wrap: break-word;
  }

  .hero-buttons {
    display: flex;
    justify-content: center;
    gap: 20px;
    flex-wrap: wrap;
  }

  .hero-buttons a {
    display: inline-block;    /* evita que un navegador ponga min-width extraño */
    max-width: 100%;
    white-space: normal;      /* permite wrap en pantallas muy pequeñas */
  }

  .btn-primary {
    background: #222222;
    color: white;
    padding: 14px 28px;
    border-radius: 8px;
    font-size: 1.1rem;
    text-decoration: none;
    transition: 0.3s;
    min-width: 0; /* importante para evitar que el botón fuerce ancho */
  }

  .btn-primary:hover {
    background: #000;
  }

  .btn-secondary {
    background: transparent;
    color: #222222;
    padding: 14px 28px;
    border: 2px solid #222222;
    border-radius: 8px;
    font-size: 1.1rem;
    text-decoration: none;
    transition: 0.3s;
    min-width: 0;
  }

  .btn-secondary:hover {
    background: #222222;
    color: white;
  }

  /* RESPONSIVE */
  @media (max-width: 768px) {
    .hero {
      padding: 0 16px; /* reduce padding en móvil */
    }

    .hero-title {
      font-size: 2.4rem;
    }

    .hero-text {
      font-size: 1.1rem;
    }
  }
</style>
