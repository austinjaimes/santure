<section>
  <div class="brands-list">
    <div class="wrapper">
      <img src="/carrusel1.png" alt="Marca 1">
      <img src="/carrusel2.png" alt="Marca 2">
      <img src="/carrusel3.png" alt="Marca 3">
      <img src="/carrusel4.png" alt="Marca 4">
      <img src="/carrusel5.png" alt="Marca 5">
      <img src="/carrusel6.png" alt="Marca 6">

      <!-- Duplicadas para scroll infinito -->
      <img src="/carrusel1.png" alt="Marca 1">
      <img src="/carrusel2.png" alt="Marca 2">
      <img src="/carrusel3.png" alt="Marca 3">
      <img src="/carrusel4.png" alt="Marca 4">
      <img src="/carrusel5.png" alt="Marca 5">
      <img src="/carrusel6.png" alt="Marca 6">
    </div>
  </div>
</section>

<style>
  .brands-list {
    overflow: hidden;
    padding: 2.5rem 0;
    background-color: #f8f8ef;
  }

  .wrapper {
    display: flex;
    gap: 3rem;
    animation: scroll 8s linear infinite;
  }

  @keyframes scroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  /* ==== TAMAÑO BASE (más grande en desktop) ==== */
  .wrapper img {
    width: 300px;    /* AUMENTADO */
    height: auto;
    flex-shrink: 0;
    transition: filter 0.3s ease;
    filter: brightness(0.8);
  }

  .wrapper img:hover {
    filter: brightness(1.1);
  }

  /* ==== RESPONSIVE ==== */

  /* Tablets y laptops pequeñas */
  @media (max-width: 1024px) {
    .wrapper img {
      width: 260px; /* Más grande que antes */
    }
  }

  /* Tablets verticales y móviles grandes */
  @media (max-width: 768px) {
    .wrapper {
      gap: 2rem;
      animation-duration: 7s;
    }

    .wrapper img {
      width: 220px; /* Más grande que antes */
    }
  }

  /* Celulares */
  @media (max-width: 480px) {
    .wrapper {
      gap: 1.2rem;
      animation-duration: 6s;
    }

    .wrapper img {
      width: 180px; /* Mucho más grande que antes */
    }
  }
</style>
